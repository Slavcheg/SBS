import {DefaultTheme} from 'react-native-paper';

const iTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: 'dodgerblue',
    accent: 'red',
    secondary: 'green',
    third: 'purple',
  },
};

export default iTheme;
